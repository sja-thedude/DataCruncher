# frozen_string_literal: true

# Convenience shim so both `require "data-cruncher"` (matching the gem name) and
# `require "data_cruncher"` (matching the module) work.
require_relative "data_cruncher"
